<Window x:Class="Kursach.View.TicketReturnWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Повернення Квитків" Height="350" Width="400"
        Background="{StaticResource BackgroundGradient}">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Повернення квитка" Style="{StaticResource HeaderTextStyle}" FontFamily="Arial Black"/>
            <ComboBox Name="TicketComboBox" Width="300" Margin="0,10">

            </ComboBox>
            <Button Content="Повернути" Width="176" Click="Return_Click" FontFamily="Arial Black" Height="37"/>
        </StackPanel>
    </Grid>
</Window>
